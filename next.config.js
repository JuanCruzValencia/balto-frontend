/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["fakestoreapi.com", "www.searchenginejournal.com"],
  },
};

module.exports = nextConfig;
